# Render.com Deployment Configuration
# https://render.com/docs/yaml-spec

services:
  # Backend API Service
  - type: web
    name: sentiment-dashboard-api
    runtime: python
    plan: starter
    buildCommand: |
      pip install --upgrade pip && \
      pip install wheel setuptools && \
      pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: PIP_NO_CACHE_DIR
        value: "1"
      - key: PIP_DISABLE_PIP_VERSION_CHECK
        value: "1"
      - key: ENVIRONMENT
        value: production
      - key: DEBUG
        value: "false"
      - key: LOG_LEVEL
        value: INFO
      - key: NEWS_API_KEY
        sync: false  # Set in Render dashboard
      - key: MINIMAX_API_KEY
        sync: false  # Set in Render dashboard
      - key: CORS_ORIGINS
        value: "*"  # Update with your frontend URL
    healthCheckPath: /health
    autoDeploy: true

  # Optional: Frontend Static Site
  # - type: web
  #   name: sentiment-dashboard-ui
  #   runtime: static
  #   buildCommand: echo "Static files ready"
 #   staticPublishPath: ./frontend
  #   routes:
 #     - type: rewrite
  #       source: /*
  #       destination: /index.html

# Cron Job for Auto-Iteration (optional)
# - type: cron
#   name: sentiment-dashboard-iteration
#   runtime: python
#   schedule: "*/15 * * * *"  # Every 15 minutes
#   buildCommand: pip install -r requirements.txt
#   startCommand: python scripts/auto_iterate.py
